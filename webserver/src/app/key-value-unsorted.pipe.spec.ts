import { KeyValueUnsortedPipe } from './key-value-unsorted.pipe';

describe('KeyValueUnsortedPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyValueUnsortedPipe();
    expect(pipe).toBeTruthy();
  });
});
